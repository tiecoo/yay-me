[build]
  command = "npm run build"
  publish = "dist/yay-me"

[functions]
  directory = "netlify/functions"

[dev]
  command = "npm start"
  targetPort = 4200
  port = 8888
  publish = "dist/yay-me"
  framework = "#custom"
